# Redis Task Status Tests

Redis task-status tests belong here. They should verify `task_id` status reads,
task-manager writes, completed-task TTL behavior, and that Redis is not used as
the message broker.
