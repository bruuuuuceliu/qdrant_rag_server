# Task Manager Tests

Task manager tests belong here. They cover task intake normalization,
`task.requests` publication, Redis status read-model updates, broker topic
wiring, and import boundaries for `task_manager_service`.
