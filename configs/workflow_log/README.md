# Workflow Log Config

This folder owns workflow-log domain service configuration. The broker-first
workflow-log server consumes workflow-log domain commands, stores durable log
entries, and publishes workflow-log domain results through Redpanda.
