# Project Config

This folder owns project-domain service configuration. The broker-first project
domain server consumes only project-domain commands and publishes project-domain
results through Redpanda.
